#!/usr/bin/make -f
# SPDX-FileCopyrightText: 2026 Skye Soss <skye@soss.website>
#
# SPDX-License-Identifier: GPL-2.0-or-later

%:
	dh $@ --buildsystem=meson

# tests/test_mountns.c needs CLONE_NEWUSER|CLONE_NEWNS privileges that
# buildd/sbuild chroots and other sandboxed package build environments
# commonly don't grant; the full suite is still run in CI/by `meson test`,
# just not as part of building the package itself.
override_dh_auto_test:
